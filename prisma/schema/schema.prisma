generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["prismaSchemaFolder"]
  output          = "./generated"
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_URI")
}
